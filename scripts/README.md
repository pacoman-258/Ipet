# Scripts

- `debug/`: temporary diagnostics, smoke tests, and manual verification helpers
- `context_summary.py`: compact project summaries for agents. Use `--repo`, `--reports`, `--data`, or `--logs` before opening large project areas.

These scripts are not part of the production runtime and should stay separate from the root entrypoints.
